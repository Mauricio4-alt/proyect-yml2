spring:
  application:
    name: proyect-yml
  mongodb:
    host: localhost
    port: 27017
    database: ejemplo01
    # username: myusername
    # password: mypassword
  data:
    mongodb:
      auto-index-creation: true
server:
  port: 8080